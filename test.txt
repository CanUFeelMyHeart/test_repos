# Работа с репозиториями GitHub

Clones a repository into a newly created directory:  
> git clone \<URL adress *.git>

Updates your current local working branch, and all of the remote tracking branches use:  
> git pull

Update remote refs along with associated objects use:  
> git push